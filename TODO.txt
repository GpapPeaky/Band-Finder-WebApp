todo:
    user psaxnei events me ola ta attributes tou band kaie event pou kanei to event 
    make guest users on login
    
    make admin on login 
    make admin be able to see everything and statystics
    admin interface on front end to rejcet/ accept reviews
    admin charts look ekfwnhsh

    send messages on band
    oi bandes mporoun na dhmiourgoun events public / private 
    guest can only see public events
    sta private events mporoun oi bandes na kanoun accept kia reject xrhstes 
    xrhstes accepted sta private events mporoun na kanoun chat
    bands na mporoun na kanoun update settings


    users can see when a band is available
    bands can regulate availability 
    bands can delete events not yet done (only public or rejected private)
    bands can update event info

    requestes private events from user require info for the event

    type sta events linked me prices baptism-700 wedding-100 party-500

    users can see events sorted by distance

API CALLS:          
DESC                                                            ROUTE                                    IN                          OUT
Get bands via event date                                        bands/event-date/:date                   event_date                  {band_name1, ...}
Get bands via event type                                        bands/event-type/:type                   event_type                  {band_name1, ...}
Get bands via event cost                                        bands/event-type/:cost                   event_cost                  {band_name1, ...}
Get events via band name if the bands is at the event           events/:band_name                        band_name                   {event_id1, event_id2}
Update private event info                                       events/update-pirv-event/:event_id       event_id                    {event_id} // The updated one.
Update band availability                                        bands/update-sched/:band_name            band_name                   {band_name} // The updated one

Chat Gtp:

    TODO – Event & Band Management Platform

    1. Βάση & Αρχιτεκτονική (Core)
    Σχεδίαση DB schema
    users (roles: admin, user, band)
    bands (profile, city, genre, year, lat/lon)
    events
    public_events
    private_events (status: requested / rejected / to_be_done / done)
    availability (band_id, date)
    messages (event_id, sender_id, receiver_id)
    reviews (pending / approved / rejected)
    payments / revenue (15% από private events done)
    Role-based access control (RBAC)
    REST API separation ανά ρόλο
    Authentication (JWT / sessions)

    2. Authentication & Users
    Guest user (χωρίς login)
    Registered user login / register
    Band login / register
    Admin login (hardcoded στη βάση)
    Διαφορετικά entry points ανά ρόλο

    3. Admin Features
    Admin dashboard (frontend)
    Διαγραφή χρηστών / bands
    Έγκριση / απόρριψη reviews
    Στατιστικά (charts)
    Bands ανά πόλη
    Public vs Private events
    Αριθμός χρηστών / bands
    Έσοδα site (15% από done private events)
    Google Charts / Chart.js integration

    4. Band Features
    Band profile (edit settings)
    Ορισμός διαθεσιμότητας (availability calendar)
    Δημιουργία public events
    Update / delete public events (μόνο αν δεν έχουν γίνει)
    Διαχείριση private event requests
    Accept / Reject με αιτιολόγηση
    Messaging με χρήστες για events σε κατάσταση to_be_done
    Προβολή:
    upcoming events
    past events (private χωρίς λεπτομέρειες)
    approved reviews

    5. Registered User Features
    Update προσωπικών στοιχείων (όχι username/email)
    Search bands με φίλτρα
    genre
    city
    year formed
    Προβολή διαθεσιμότητας μπάντας
    Αίτηση για private event
    event type (baptism / wedding / party)
    auto price assignment
    baptism → 700
    wedding → 1000
    party → 500
    Διαχείριση δικών του private events
    Messaging με μπάντα (μόνο to_be_done)
    Υποβολή review (pending approval)
    Προβολή public events sorted by distance
    Distance via coordinates
    Distance by car (TrueWay Matrix API)

    6. Guest Features
    Προβολή bands (read-only)
    Search bands με φίλτρα
    Προβολή public events
    φίλτρα: ημερομηνία, πόλη, είδος
    Προβολή public events σε χάρτη
    Πρόσβαση σε links / footer info
    No access σε private events, availability, messaging

    7. Events Logic
    Public & private events χωρίς ημερομηνία στο creation
    Date validation με availability της μπάντας
    Status flow:
    requested → rejected
    requested → to_be_done → done
    User μπορεί να κάνει done μετά την ημερομηνία
    Band μπορεί να διαγράψει μόνο:
    public events (future)
    private events (rejected)

    8. Search & AI
    Natural language search → SQL
    Pass DB schema + question σε LLM
    Execute generated SQL safely
    Music Q&A LLM (free model)
    Example prompts
    Hate speech detection σε messages & reviews
    Auto reject αν εντοπιστεί

    9. Maps & Distance
    Lat/Lon αποθήκευση σε users & events
    Map view για public events
    Distance sorting
    Car distance via TrueWay API

    10. Bonus
    Logging επισκέψεων σε band profile
    total visits
    distinct registered users
    Band analytics panel

    11. Frontend
    Διαφορετικά dashboards ανά ρόλο
    Ajax για:
    messaging
    reviews
    availability
    search
    Responsive design
    Clear separation guest / user / band / admin UI

    12. Τελική Αναφορά
    Αρχιτεκτονική συστήματος
    API endpoints & responsibilities
    Libraries & frameworks
    Client-side JS logic
    REST usage
    DB changes
    AI integration

